import { Component } from '../../core';

export class MainTemplate extends Component {
  getTemplate(): string {
    return `<main class="main">
    <!-- MainMenuController -->

    <!-- MainSortController -->

    <!-- Контент -->
  </main>`.trim();
  }
}
